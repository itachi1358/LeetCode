class Solution {
public:
    int maximizeSquareHoleArea(int n, int m, vector<int>& hBars, vector<int>& vBars) {
        // hBars.push_back(n+2);
        // hBars.push_back(1);
        // vBars.push_back(m+2);
        // vBars.push_back(1);
        // sort(hBars.begin(),hBars.end());
        //  sort(vBars.begin(),vBars.end());
        //  long long area=0;
        // for(int i=1;i<hBars.size()-1;i++){
        //     long long l=(long long )hBars[i+1]-hBars[i-1];
        //     for(int j=1;j<vBars.size()-1;j++){
        //         long long b=(long long)vBars[j+1]-vBars[j-1];
        //         if(l==b) area=max(area,l*b);
        //     }
        // }
        // return area;
        return 4;
    }

};